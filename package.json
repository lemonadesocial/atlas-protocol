{
  "name": "atlas-protocol",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "ATLAS Protocol — open agent commerce protocol for events.",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "tsc -b",
    "test": "pnpm -r test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -b --noEmit",
    "clean": "tsc -b --clean && rm -rf packages/*/dist examples/*/dist packages/*/*.tsbuildinfo examples/*/*.tsbuildinfo",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0",
    "@types/node": "^25.7.0",
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "husky": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.59.3"
  }
}
