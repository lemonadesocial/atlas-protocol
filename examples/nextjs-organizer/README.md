# Example — Next.js organizer app

A Next.js reference app that uses `@atlas/server-sdk` to expose the well-known endpoints, registry routes, and HTTP 402 purchase flow needed for an organizer to be discoverable and bookable by ATLAS-aware agents.

Coming soon. The runnable example will land here once `@atlas/server-sdk` ships.
