# Example — LangChain agent

A LangChain agent wired up against the ATLAS toolset, demonstrating the discover → quote → purchase → receipt loop with no custom protocol code.

Coming soon. The runnable example will land here once `@atlasprotocol/agent-tools` ships its LangChain bindings.
