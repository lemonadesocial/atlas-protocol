# Example — Desktop MCP integration

A minimal MCP server that exposes the ATLAS agent toolset to a desktop MCP host, so a user can ask their assistant to discover events, fetch ticket types, and complete an HTTP 402 purchase end-to-end.

Coming soon. The runnable example will land here once `@atlas/agent-tools` ships.
