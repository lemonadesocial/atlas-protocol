# @atlas/server-sdk

This package will host the reference server implementation — the well-known endpoints, registry API, and HTTP 402 purchase flow defined in [`specs/01-PROTOCOL-SPEC.md`](../../specs/01-PROTOCOL-SPEC.md), packaged so any platform integrator can stand up an ATLAS-compliant backend without reimplementing the protocol from scratch. Coming soon.
